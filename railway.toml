[build]
builder = "NIXPACKS"
buildCommand = "cd frontend && rm -f package-lock.json && npm install && npm run build"

[deploy]
startCommand = "cd frontend && npx serve -s dist -l $PORT"
restartPolicyType = "ON_FAILURE"

[nixpacks.variables]
NODE_VERSION = "20"
